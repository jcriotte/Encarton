{% extends 'layout.html.twig' %}

{% block content %}
    <main class='container-artist'>

        {# listes album #}
        <section class='albums'>
            <h2>Résultat<h2>
                <ul>
                    {% for album in albums %}
                    <li>{{ album.name }}</li>
                    <li>{{ album.photo }}</li>
                    {% endfor %}
                </ul>
        </section>
    </main>