{% extends 'layoutEnCarton.html.twig' %}

{% block content %}
    <main>
        {# liste albums #}
        <section class='result-albums'>
            <h2>Résultat<h2>
                <ul>
                    {% for album in albums %}
                    <li>{{ album.name }}</li>
                    <li>{{ album.photo }}</li>
                    {% endfor %}
                </ul>
        </section>
    </main>
{% endblock %}